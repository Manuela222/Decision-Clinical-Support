{
  "name": "cds-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend for the discharge medication recommendation prototype (Phase 17). Calls the FastAPI backend only.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
